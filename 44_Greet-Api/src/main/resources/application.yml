management:
  endpoints:
    web:
      exposure:
        exclude: '*'
server:
  port: 2222
spring:
  boot:
    admin:
      client:
        url: http://localhost:9090/
        
  application:
    name: GREET-API